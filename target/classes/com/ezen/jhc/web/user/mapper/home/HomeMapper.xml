<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezen.jhc.web.user.mapper.home.HomeMapper">

	<select id="get_review_list"
		resultType="com.ezen.jhc.web.user.mapper.home.HomeMapper">
		SELECT review_num, review_image_path, review_content, review_star, review_regdate, mem_name
		 FROM reviews r, members m WHERE r.mem_num = m.mem_num AND review_image_path IS NOT NULL
	</select>

</mapper>